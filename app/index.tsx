import { WorkOrderListScreen } from '@/features/work-orders/list/screens/WorkOrderListScreen';

/**
 * Home route — work-order list (Screen 1).
 */
export default function Index() {
  return <WorkOrderListScreen />;
}
